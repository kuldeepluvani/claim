{
  "name": "claim",
  "version": "3.0.0-alpha.1",
  "description": "Developer Knowledge OS — persistent memory for AI coding agents",
  "type": "module",
  "bin": {
    "claim": "./dist/cli/index.js"
  },
  "scripts": {
    "dev": "bun run --watch src/cli/index.ts",
    "build": "bun build src/cli/index.ts --outdir dist/cli --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["claude", "memory", "knowledge", "obsidian", "ai", "developer-tools"],
  "author": "Kuldeep Luvani",
  "license": "MIT",
  "engines": {
    "bun": ">=1.2.0"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7"
  }
}
